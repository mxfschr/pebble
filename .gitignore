node_modules/
dist/

# Pebble: DB + machine-specific config are private,
# but memory.md and context-tree/ are git-trackable (shareable knowledge).
.pebble/memory.db*
.pebble/config.json
.pebble/run.sh

# Per-machine MCP config (hardcoded paths)
.mcp.json

*.db
.claude/settings.local.json

# Positioning work — internal strategy docs (competitive analysis,
# evidence log, roadmap). Kept private to avoid leaking competitive
# intel + maintainer voice notes. Lives only on the maintainer's machine.
positioning/
